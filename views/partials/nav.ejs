<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">

        <!-- Website logo or title -->
        <a class="navbar-brand" href="/">
            <img src="/images/logo.png" alt="PerfectPlanner Logo" height="35" class="me-2">
            PerfectPlanner
        </a>

        <!-- menu button for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

                <!-- SHOW ONLY IF USER IS LOGGED IN -->
                <% if (isAuthenticated) { %>

                    <!-- Home -->
                    <li class="nav-item">
                        <% if (title=="Home") { %>
                            <a class="nav-link active" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
                        <% } else { %>
                            <a class="nav-link" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
                        <% } %>
                    </li>

                    <!-- Add Event -->
                    <li class="nav-item">
                        <% if (title=="Add Event") { %>
                            <a class="nav-link active" href="/events/create"><i class="fa-solid fa-calendar-plus"></i> Add Event</a>
                        <% } else { %>
                            <a class="nav-link" href="/events/create"><i class="fa-solid fa-calendar-plus"></i> Add Event</a>
                        <% } %>
                    </li>

                    <!-- Events List -->
                    <li class="nav-item">
                        <% if (title=="Events") { %>
                            <a class="nav-link active" href="/events/list"><i class="fa-solid fa-calendar-check"></i> Events</a>
                        <% } else { %>
                            <a class="nav-link" href="/events/list"><i class="fa-solid fa-calendar-check"></i> Events</a>
                        <% } %>
                    </li>

                    <!-- 🔥 PROFILE LINK ADDED HERE -->
                    <li class="nav-item">
                        <% if (title=="Profile") { %>
                            <a class="nav-link active" href="/profile"><i class="fa-solid fa-user"></i> Profile</a>
                        <% } else { %>
                            <a class="nav-link" href="/profile"><i class="fa-solid fa-user"></i> Profile</a>
                        <% } %>
                    </li>

                    <!-- Logout -->
                    <li class="nav-item">
                        <form action="/auth/logout" method="POST">
                            <button class="btn nav-link" style="color:white">
                                <i class="fa-solid fa-right-from-bracket"></i> Logout
                            </button>
                        </form>
                    </li>

                <% } else { %>  
                    <!-- SHOW ONLY IF USER IS NOT LOGGED IN -->
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login"><i class="fa-solid fa-right-to-bracket"></i> Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/register"><i class="fa-solid fa-user-plus"></i> Register</a>
                    </li>
                <% } %>

            </ul>
        </div>
    </div>
</nav>
