<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <!-- Website logo or title -->
        <a class="navbar-brand" href="/">
            <img src="/images/logo.png" alt="PerfectPlanner Logo" height="35" class="me-2">
            PerfectPlanner
        </a>

        <!-- menu button for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible navigation -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <!-- Home -->
                <li class="nav-item">
                    <% if (title=="Home") { %>
                        <a class="nav-link active" aria-current="page" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
                    <% } %>
                </li>

                <!-- Add Event -->
                <li class="nav-item">
                    <% if (title=="Add Event") { %>
                        <a class="nav-link active" aria-current="page" href="/events/create"><i class="fa-solid fa-calendar-plus"></i> Add Event</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/events/create"><i class="fa-solid fa-calendar-plus"></i> Add Event</a>
                    <% } %>
                </li>

                <!-- Events List -->
                <li class="nav-item">
                    <% if (title=="Events") { %>
                        <a class="nav-link active" aria-current="page" href="/events/list"><i class="fa-solid fa-calendar-check"></i> Events</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/events/list"><i class="fa-solid fa-calendar-check"></i> Events</a>
                    <% } %>
                </li>
            </ul>
        </div>
    </div>
</nav>