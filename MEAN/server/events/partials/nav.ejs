<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <!-- Website logo or title that links back to the homepage -->
        <a class="navbar-brand" href="/">
            <img src="/images/logo.png" alt="PerfectPlanner Logo" height="35" class="me-2">
            PerfectPlanner
        </a>
        <!-- menu button for mobile view -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <!-- Collapsible navigation section -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <!-- Home link — highlighted when the current page title is 'Home' -->
                <li class="nav-item">
                    <% if (title=="Home" ) { %>
                        <a class="nav-link active" aria-current="page" href="/index"><i class="fa-solid fa-lg fa-house"></i>Home</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/index"><i class="fa-solid fa-lg fa-house"></i> Home</a>
                    <% } %>
                </li>
                <!-- Create Event link: highlighted if current page is Create Event -->
                <li class="nav-item">
                    <% if (title=="Create Event" ) { %>
                        <a class="nav-link active" aria-current="page" href="/create"><i class="fa-solid fa-calendar-plus"></i>Create</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/create"><i class="fa-solid fa-calendar-plus"></i>Create</a>
                    <% } %>
                </li>
                <!-- View Event link — highlighted when the current page title is 'Events' -->
                <li class="nav-item">
                    <% if (title=="Events" ) { %>
                        <a class="nav-link active" aria-current="page" href="/list"><i class="fa-solid fa-calendar-check"></i>Events</a>
                    <% } else { %>
                        <a class="nav-link" aria-current="page" href="/list"><i class="fa-solid fa-calendar-check"></i>Events</a>
                    <% } %>
                </li>
            </ul>
        </div>
    </div>
</nav>